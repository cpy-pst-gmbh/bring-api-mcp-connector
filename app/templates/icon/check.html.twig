{#
  Material Symbols "check", inlined — same reasoning as the gear next to it:
  the icon font would be a few hundred kilobytes and a third party on every
  page for one glyph. viewBox is Material's own (0 -960 960 960).
#}
<svg class="{{ class|default('size-5') }}" viewBox="0 -960 960 960" fill="currentColor" aria-hidden="true">
    <path d="M382-240 154-468l57-57 171 171 367-367 57 57-424 424Z" />
</svg>
