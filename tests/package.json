{
    "name": "bring-mcp-e2e",
    "private": true,
    "description": "End-to-end tests driving both containers.",
    "scripts": {
        "test": "playwright test",
        "report": "playwright show-report"
    },
    "devDependencies": {
        "@playwright/test": "^1.50.0"
    }
}
